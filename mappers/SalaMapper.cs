using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using movies_api.dtos;
using movies_api.models;

namespace movies_api.mappers
{
    public class SalaMapper
    {
        /* public static ToSalaDTO(Sala SalaModel)
        {
            return new SalaDTO
            {
                Id = SalaModel.Id,
                Nome = SalaModel.Nome,
                Capacidade = SalaModel.Capacidade
            };
        }

        public static Sala ToSalaModel(SalaDTO SalaDTO)
        {
            return new Sala
            {
                Nome = SalaDTO.Nome,
                Capacidade = SalaDTO.Capacidade,
                Assentos = SalaDTO.Assentos,
                Sessoes = SalaDTO.Sessoes
            };
        } */
    }
}